// Minimal Tailwind CSS v4 config.
// Config is optional in v4; your project already works via @tailwindcss/postcss and CSS.
// Add theme extensions or plugins here if needed.

const config = {
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
